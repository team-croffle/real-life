# 에이전트 지침

NestJS 12 + Vue 3 pnpm 모노레포다. 워크스페이스는 `server`(API), `web`(클라이언트), `shared`(타입·상수)다. 스택, 스크립트, 디렉터리 구조는 [README.md](README.md)를 본다. 이 문서에 README를 복제하지 않는다.

## 로컬 `.ai` 폴더

`.ai/`는 **로컬 전용**이며 gitignore 되어 있다.

- `.ai/`와 아래 파일이 없으면 만든다.
- `.ai/**`를 스테이징, 커밋, 푸시하지 않는다. 기능명세(`.ai/specs/`)도 여기에 포함한다.
- 디스크에 두고 싶지 않은 시크릿을 `.ai/`에 넣지 않는다.

## ROADMAP (작업 전 필수)

로드맵은 두 개다. 섞지 않는다.

| 파일                               | 용도                                     | Git           |
| ---------------------------------- | ---------------------------------------- | ------------- |
| [`.ai/ROADMAP.md`](.ai/ROADMAP.md) | 로컬 작업용. 전체 표 + 버전별 체크리스트 | 커밋하지 않음 |
| [`ROADMAP.md`](ROADMAP.md)         | GitHub 공개용. 버전·한 줄 목표·상태만    | 커밋 가능     |

로컬 목차(전체 / 버전별)와 공개 표 구성을 에이전트가 갈아끼우지 않는다. 로컬 세부 항목을 공개 파일에 풀어 쓰지 않는다.

**작업을 시작하기 전에:**

1. 둘 다 읽는다. 구현 범위는 **로컬** 버전별 체크리스트가 기준이다.
2. 로컬 표에서 현재 버전을 고른다. `진행 중`이 있으면 그것, 없으면 아직 끝나지 않은 가장 앞 버전.
3. 해당 버전의 [기능명세](.ai/specs/features.md)를 읽는다. 없으면 구현하지 않고 사용자에게 알린다.
4. 해당 버전 항목과 맞춘다. 범위가 바뀌면 로컬 섹션·표 상태를 먼저 고친 뒤 구현한다. 공개 표의 버전 상태도 같이 맞춘다.

**작업 중·작업 후:**

- 로컬: 끝난 항목은 `[x]`. 표 상태 `진행 예정` / `진행 중` / `완료`.
- 공개: 같은 버전의 **상태**만 맞춘다. 목표 한 줄이 실제로 바뀐 경우에만 고친다.
- 새 할 일은 로컬 해당 버전 섹션에 넣는다. 버전을 건너뛰지 않는다.

## 기능명세 (구현 전 필수)

경로: [`.ai/specs/features.md`](.ai/specs/features.md) (로컬 전용, 커밋하지 않음).

역할이 다르다.

- **ROADMAP**: 무엇을 만드는지 (버전 범위, 체크리스트).
- **기능명세**: 어떻게 동작하는지 (해당 버전 섹션의 동작·규칙).

**구현 전에** 지금 다루는 버전의 명세를 확인한다. 명세가 동작을 정한다.

명세와 다르게 구현해야 할 이유가 생기면 임의로 진행하지 말고 **먼저 사용자에게 알린다.** 명세 수정은 **사용자 확인 후에만** 한다. 확인 없이 `.ai/specs/`를 고치지 않는다.

## 작업 내역 (필수)

경로: `.ai/history/`

작업 단위가 끝날 때, 그리고 세션이 끝날 때 상세히 기록한다.

- `.ai/history/YYYY-MM-DD.md`에 이어 쓰거나, 단위가 뚜렷하면 `.ai/history/YYYY-MM-DD-short-slug.md`를 쓴다.
- 각 항목에 넣을 것: 시각, 목표, 무엇이 바뀌었는지(파일과 동작), 결정, 남은 리스크나 할 일.
- 이 채팅 없이도 다음 세션이 이어갈 수 있을 만큼 쓴다.

## 브랜치와 커밋

브랜치 생성·전환은 **사용자가 한다.** 에이전트는 `git checkout`, `git switch`, `git branch`로 브랜치를 만들지 않고, **지금 체크아웃된 브랜치에서만** 작업한다.

브랜치는 서버와 클라이언트를 분리해서 진행한다. 같은 브랜치에 서버 작업과 클라이언트 작업을 함께 넣지 않는다. 예: `feat/auth-server` → `feat/auth-web`.

파일명·함수명·타입명은 실제 동작과 일치해야 한다. 예: JWT를 localStorage에 저장하는 파일을 `session.ts`라고 부르지 않는다.

커밋 전에 스스로 점검한다. 단일 책임 원칙 위반, 불필요하게 복잡한 구조, 중복 로직이 있으면 커밋 전에 먼저 알린다.

작업은 `feat/*` 브랜치에서 한다. **`master`에 직접 커밋하지 않는다.** 현재 브랜치가 `master`이면 커밋하지 말고 사용자에게 `feat/*`로 바꿔 달라고 한다. 사용자가 `master`에 커밋하라고 명시한 경우(초기 세팅 등)만 예외다.

긴 작업은 논리 단위로 나눈다 (커밋당 한 가지 관심사. 예: 스키마, 그다음 API, 그다음 UI). 마지막에 한 커밋으로 몰지 않는다.

커밋 전 확인 절차는 로컬 지침을 따른다.

push는 **사용자가 요청할 때만** 한다. 에이전트가 먼저 push하지 않는다.

`.ai/`와 시크릿(`.env`, 자격 증명)은 커밋하지 않는다. git 훅을 건너뛰지 않는다. `master`에 force-push하지 않는다.

커밋 메시지: 제목(영어) + 요약(한글) + 상세 불릿(한글).

```
<type>: <short title>

<이 단위를 커밋하는 이유 요약>

- <구체적인 변경>
- <구체적인 변경>
- <후속이나 제약 있으면>
```

예시:

```
feat: add user list API

사용자 목록 조회를 서버에 추가한다.

- GET /users 엔드포인트와 DTO를 추가한다.
- 페이지네이션 쿼리를 기존 목록 패턴에 맞춘다.
```

`type` 예: `feat`, `fix`, `refactor`, `chore`, `docs`. 제목은 영어 명령형. 요약과 불릿은 한글.
